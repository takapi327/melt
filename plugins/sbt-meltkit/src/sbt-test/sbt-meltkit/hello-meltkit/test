# Verify that meltMode := Some(Browser) sets meltCodegenMode to "spa"
# and meltGenerate produces the expected Scala file from App.melt
> meltGenerate
$ exists target/out/sjs1/scala-3.8.4/hello-meltkit/src_managed/main/melt/components/App.scala
# Verify the full project compiles (includes the generated App.scala + Main.scala)
> compile

# Verify Scala.js linking succeeds
> fastLinkJS
