# Verify that meltMode := Some(Browser) sets meltcCodegenMode to "spa"
# and meltcGenerate produces the expected Scala file from App.melt
> meltcGenerate
$ exists target/scala-3.8.3/src_managed/main/meltc/components/App.scala
# Verify the full project compiles (includes the generated App.scala + Main.scala)
> compile

# Verify Scala.js linking succeeds
> fastLinkJS
