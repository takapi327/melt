<script lang="scala">
  import model.User

  case class Props(users: List[User] = Nil)
</script>

<main class="page">
  <h1>
    Users
  </h1>
  <p class="hint">
    Every link below is checked at compile time against <code>routes.Routes</code> — the static <code>/users</code> as well as the parameterised <code>/users/{"{id}"}</code>. Misspell either path and this file no longer compiles: a broken internal link is a build error, not a 404.
  </p>
  <p class="hint">
    <a href="/users">Reload this list</a> — a wholly literal link, checked against <code>Routes.list</code>.
  </p>
  <ul class="user-list">
    {props.users.map(u =>
      <li>
        <a href="{u.id}">{u.name}</a>
        <span class="email">{u.email}</span>
      </li>
    )}
  </ul>
</main>

<style>
  .page {
    max-width: 40rem;
    margin: 3rem auto;
    font-family: system-ui, sans-serif;
    color: #1f2933;
  }

  h1 {
    color: #ff3e00;
    margin-bottom: 0.25rem;
  }

  .hint {
    font-size: 0.85rem;
    color: #616e7c;
    background: #f5f7fa;
    border-radius: 0.5rem;
    padding: 0.75rem 1rem;
  }

  code {
    background: #e4e7eb;
    border-radius: 0.25rem;
    padding: 0 0.25rem;
  }

  .user-list {
    list-style: none;
    padding: 0;
  }

  .user-list li {
    display: flex;
    align-items: baseline;
    gap: 0.75rem;
    padding: 0.6rem 0;
    border-bottom: 1px solid #e4e7eb;
  }

  .user-list a {
    font-weight: 600;
    color: #0b7285;
    text-decoration: none;
  }

  .user-list a:hover {
    text-decoration: underline;
  }

  .email {
    font-size: 0.85rem;
    color: #9aa5b1;
  }
</style>
